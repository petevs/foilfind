import { Text } from "@mantine/core"
import { Box, Title } from "tabler-icons-react"

const SectionTitle = ({ title }) => {

    return (
        <Box>
            <Text>{title}</Text>
        </Box>
    )
}

export default SectionTitle